/**
 * Central application configuration.
 * Update API_BASE_URL here to change the backend URL across the entire frontend.
 */
const APP_CONFIG = {
    API_BASE_URL: "http://localhost:8081",
};

export default APP_CONFIG;
